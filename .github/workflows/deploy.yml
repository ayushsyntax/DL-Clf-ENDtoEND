name: Deploy to ECS

on:
  workflow_dispatch:
  # push:
  #   branches:
  #     - main

env:
  ECS_CLUSTER: brain-tumor-cluster
  ECS_SERVICE: brain-tumor-service
  ECS_TASK_FAMILY: brain-tumor-inference-task
  CONTAINER_NAME: brain-tumor-container
  NEW_REVISION: ""

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment paused
        run: echo "Deploy pipeline is paused. Uncomment steps below and change trigger to push to re-enable."

      # To re-enable full deployment, uncomment all steps below and change 'on' trigger to push.
      # - name: 1. Checkout
      #   uses: actions/checkout@v4

#       - name: 2. Configure AWS credentials
#         uses: aws-actions/configure-aws-credentials@v4
#         with:
#           aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           aws-region: ${{ secrets.AWS_REGION }}

#       - name: 3. Login to Amazon ECR
#         id: login-ecr
#         uses: aws-actions/amazon-ecr-login@v2

#       - name: 4. Download model
#         run: |
#           mkdir -p artifacts
#           aws s3 cp s3://${{ secrets.AWS_S3_BUCKET }}/models/best_model.keras artifacts/best_model.keras

#       - name: 5. Build Docker image
#         env:
#           ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
#           ECR_REPOSITORY: ${{ secrets.ECR_REPOSITORY }}
#           IMAGE_TAG: ${{ github.sha }}
#         run: |
#           docker build -f infra/Dockerfile -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG -t $ECR_REGISTRY/$ECR_REPOSITORY:latest .

#       - name: 6. Push to ECR
#         env:
#           ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
#           ECR_REPOSITORY: ${{ secrets.ECR_REPOSITORY }}
#           IMAGE_TAG: ${{ github.sha }}
#         run: |
#           docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
#           docker push $ECR_REGISTRY/$ECR_REPOSITORY:latest

#       - name: 7. Register new ECS task definition
#         env:
#           ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
#           ECR_REPOSITORY: ${{ secrets.ECR_REPOSITORY }}
#           IMAGE_TAG: ${{ github.sha }}
#         run: |
#           aws ecs describe-task-definition --task-definition ${{ env.ECS_TASK_FAMILY }} --query taskDefinition > task-def.json

#           # Update the image URI inline using jq, and strip out fields that cannot be included in register-task-definition
#           jq '.containerDefinitions[0].image = "'"$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG"'" | del(.taskDefinitionArn, .revision, .status, .requiresAttributes, .compatibilities, .registeredAt, .registeredBy)' task-def.json > new-task-def.json

#           NEW_TASK_INFO=$(aws ecs register-task-definition --cli-input-json file://new-task-def.json)
#           NEW_REVISION=$(echo $NEW_TASK_INFO | jq -r '.taskDefinition.revision')
#           echo "NEW_REVISION=$NEW_REVISION" >> $GITHUB_ENV

#       - name: 8. Update ECS service
#         run: |
#           aws ecs update-service \
#             --cluster "$ECS_CLUSTER" \
#             --service "$ECS_SERVICE" \
#             --task-definition "$ECS_TASK_FAMILY:$NEW_REVISION" \
#             --force-new-deployment
#         env:
#           NEW_REVISION: ${{ env.NEW_REVISION }}


#       - name: 9. Wait for service stability
#         run: |
#           aws ecs wait services-stable \
#             --cluster "$ECS_CLUSTER" \
#             --services "$ECS_SERVICE"

#       - name: 10. Get public IP
#         run: |
#           TASK_ARN=$(aws ecs list-tasks --cluster "$ECS_CLUSTER" --service-name "$ECS_SERVICE" --desired-status RUNNING --query 'taskArns[0]' --output text)

#           if [ "$TASK_ARN" == "None" ] || [ -z "$TASK_ARN" ]; then
#             echo "No running tasks found."
#             exit 1
#           fi

#           ENI_ID=$(aws ecs describe-tasks --cluster "$ECS_CLUSTER" --tasks "$TASK_ARN" --query 'tasks[0].attachments[0].details[?name==`networkInterfaceId`].value' --output text)

#           PUBLIC_IP=$(aws ec2 describe-network-interfaces --network-interface-ids "$ENI_ID" --query 'NetworkInterfaces[0].Association.PublicIp' --output text)

#           echo "Live endpoint: http://$PUBLIC_IP:8000"
#           echo "Test with: curl http://$PUBLIC_IP:8000/health"
